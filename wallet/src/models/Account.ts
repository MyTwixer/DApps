// Define the WalletInfo type
export interface WalletInfo {
    seedPhrase: string;
    privateKey: string;
    address: string;
    balance: string;
}

export interface AccountInfo {
    privateKey: string;
    address: string;
    balance: string;
}